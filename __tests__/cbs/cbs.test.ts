// @ts-nocheck -- test runner (Jest/Vitest) not configured yet.
import { cbsClient } from "@/lib/cbs";

describe("cbsClient", () => {
  it.todo("fetches a CPI dataset for a period range");
  it.todo("caches repeated requests for the same range");
  it.todo("returns one CpiDataPoint per COICOP category per month");
});
