// @ts-nocheck -- test runner (Jest/Vitest) not configured yet.
import { keywordCategorizer } from "@/lib/categorizer";

describe("keywordCategorizer", () => {
  it.todo("assigns a COICOP code when a keyword rule matches");
  it.todo("leaves coicop null when nothing matches");
  it.todo("prefers the rule with the highest weight on conflicts");
});
